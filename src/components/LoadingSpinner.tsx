// LoadingSpinner.tsx
import React from "react";

const LoadingSpinner: React.FC = () => {
  return (
    <div className="spinner-container">
      {/* Add your loading spinner styles or an actual spinner component here */}
      Loading...
    </div>
  );
};

export default LoadingSpinner;
